//
//  DeviceManagerView.swift
//  NextEld
//
//  Created by priyanshi   on 17/06/25.
//

/*import Foundation
import SwiftUI

struct DeviceManagerView: View {
    @StateObject var viewModel = PT30DataViewModel()
   // @StateObject var viewModel = PT30DataViewModel.shared

    var body: some View {
        VStack(spacing: 16) {
            Text("PT30 Live Dashboard")
                .font(.title2)
                .bold()

            HStack {
                Text("Temperature:")
                Spacer()
                Text(viewModel.description)
            }

            HStack {
                Text("Fuel Level:")
                Spacer()
                Text(viewModel.connectionStatus)
            }

            HStack {
                Text("Battery:")
                Spacer()
                Text(viewModel.connectionStatus)
            }

            HStack {
                Text("Speed:")
                Spacer()
                Text(viewModel.connectionStatus)
            }
        }
        .padding()
        .background(RoundedRectangle(cornerRadius: 12).stroke(Color.blue, lineWidth: 1))
        .padding()
    }
}
struct PT30ConnectionView_Previews: PreviewProvider {
    static var previews: some View {
        PT30ConnectionView()
    }
}
*/
