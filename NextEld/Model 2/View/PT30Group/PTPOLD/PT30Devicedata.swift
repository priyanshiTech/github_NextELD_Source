//
//  PT30Devicedata.swift
//  NextEld
//
//  Created by priyanshi   on 17/06/25.
//

/*import Foundation
import SwiftUI

struct PT30DeviceDataView: View {
  //  @StateObject var viewModel = PT30DataViewModel.shared
    @StateObject var viewModel = PT30DataViewModel()


    var body: some View {
        VStack(spacing: 16) {
            Text("PT30 Device Live Data")
                .font(.title2).bold()

            deviceRow(title: "Temperature", value: viewModel.connectionStatus)
            deviceRow(title: "Fuel Level",   value: viewModel.connectionStatus)
            deviceRow(title: "Battery", value: viewModel.connectionStatus)
            deviceRow(title: "Speed",        value: viewModel.connectionStatus)

            Spacer()
        }
        .padding()
    }



    
    @ViewBuilder
    private func deviceRow(title: String, value: String) -> some View {
        HStack {
            Text(title)
                .font(.headline)
            Spacer()
            Text(value)
                .font(.body)
                .foregroundColor(.blue)
        }
        .padding(.horizontal)
    }
}
*/
